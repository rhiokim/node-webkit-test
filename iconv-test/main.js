//var Iconv = require('iconv').Iconv;
var fs = require('fs');

fs.readFile('./test.smi', function(err, data) {
 // var iconv = new Iconv('ISO-2022-JP', 'UTF-8');
//  var res = iconv.convert(data);

//  document.body.innerHTML = res;
});
